---
# ServiceMonitor for Prometheus Operator auto-discovery of controller metrics.
# Requires the Prometheus Operator CRDs to be installed in the cluster.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: csi-azurelustre-controller
  namespace: kube-system
  labels:
    app: csi-azurelustre-controller
    release: prometheus
spec:
  selector:
    matchLabels:
      app: csi-azurelustre-controller
  namespaceSelector:
    matchNames:
      - kube-system
  endpoints:
    - port: metrics
      interval: 15s
